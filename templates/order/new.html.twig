{% extends 'base.html.twig' %}

{% block title %}{{parent()}} - Confirmation réservation{% endblock %}

{% block body %}
<div class="container mt-4 col-md-6">
    <h1>Vérifier et confirmer la réservation</h1>
    {{ include('order/_form.html.twig', {'button_label': 'Réserver'}) }}

    <a href="{{ path('home') }}" class="btn btn-primary" title="retour à l'accueil"><i class="fas fa-arrow-left me-2"></i>Retour</a>
</div>
</div>
{% endblock %}
